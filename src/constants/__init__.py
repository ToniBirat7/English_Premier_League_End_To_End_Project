# This is an init file for the package
# __init__.py file

from pathlib import Path

CONFIG_FILE_PATH = Path("config/config.yaml") # As our code will run from the root directory i.e. `main.py`
PARAMS_FILE_PATH = Path("params.yaml") # As our code will run from the root directory i.e. `main.py`
SCHEMA_FILE_PATH = Path("schema.yaml") # As our code will run from the root directory i.e. `main.py`