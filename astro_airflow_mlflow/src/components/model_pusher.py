# model_pusher.py file
# Registration to the MLflow model registry is currently performed inline
# inside dags/load_and_test_best_model.py. This component is intentionally
# left empty until that logic is extracted; do not import from it.
